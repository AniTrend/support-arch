# annotation/src/

## Responsibility

Groups annotation source sets. Only `main` contains production Kotlin code in this scope.

## Design Patterns

Kotlin JVM source-set boundary.

## Data & Control Flow

Gradle compiles `main` sources into the annotation artifact.

## Integration Points

See `main/codemap.md`.
