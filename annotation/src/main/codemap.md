# annotation/src/main/

## Responsibility

Production source set for the annotation artifact.

## Design Patterns

Plain Kotlin JVM layout with sources under `kotlin`.

## Data & Control Flow

The Kotlin compiler emits annotation classes consumed by KSP in downstream modules.

## Integration Points

See `kotlin/co/anitrend/arch/annotation/codemap.md`.
