# buildSrc/src/main/java/

## Responsibility

Source root for buildSrc plugin implementation files.

## Design Patterns

The directory name is `java`, but the implementation files are Kotlin.

## Data & Control Flow

No behavior lives at this root. Continue to `co/anitrend/arch/buildSrc`.

## Integration Points

See `co/anitrend/arch/buildSrc/codemap.md`.
