# processor/src/

## Responsibility

Groups processor source sets. This codemap covers production code only.

## Design Patterns

Kotlin JVM source-set boundary.

## Data & Control Flow

Gradle compiles `main` sources into the processor artifact. Test sources are intentionally not mapped here.

## Integration Points

See `main/codemap.md`.
