# paging-legacy/src/main/kotlin/co/anitrend/

## Responsibility

Intermediate codemap for `paging-legacy/src/main/kotlin/co/anitrend/`. Namespace path for the legacy paging package.

## Design Patterns

Directory pointer only. It keeps navigation explicit while the implementation details live in child package codemaps.

## Data & Control Flow

Control moves through this folder into `paging-legacy/src/main/kotlin/co/anitrend/arch/paging/` and, when present, sibling resource codemaps.

## Integration Points

Use `paging-legacy/src/main/kotlin/co/anitrend/arch/paging/` for the meaningful Kotlin package map and nearby `res/` codemaps for Android resources.
