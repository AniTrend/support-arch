# core/src/main/kotlin/co/anitrend/arch/

## Responsibility

Intermediate codemap for `core/src/main/kotlin/co/anitrend/arch/`. Namespace path for the public core package.

## Design Patterns

Directory pointer only. It keeps navigation explicit while the implementation details live in child package codemaps.

## Data & Control Flow

Control moves through this folder into `core/src/main/kotlin/co/anitrend/arch/core/` and, when present, sibling resource codemaps.

## Integration Points

Use `core/src/main/kotlin/co/anitrend/arch/core/` for the meaningful Kotlin package map and nearby `res/` codemaps for Android resources.
