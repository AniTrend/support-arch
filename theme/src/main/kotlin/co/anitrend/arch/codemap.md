# theme/src/main/kotlin/co/anitrend/arch/

## Responsibility

Intermediate codemap for `theme/src/main/kotlin/co/anitrend/arch/`. Namespace path for the public theme package.

## Design Patterns

Directory pointer only. It keeps navigation explicit while the implementation details live in child package codemaps.

## Data & Control Flow

Control moves through this folder into `theme/src/main/kotlin/co/anitrend/arch/theme/` and, when present, sibling resource codemaps.

## Integration Points

Use `theme/src/main/kotlin/co/anitrend/arch/theme/` for the meaningful Kotlin package map and nearby `res/` codemaps for Android resources.
