# ui/src/main/kotlin/co/

## Responsibility

Intermediate codemap for `ui/src/main/kotlin/co/`. Namespace path for the public ui package.

## Design Patterns

Directory pointer only. It keeps navigation explicit while the implementation details live in child package codemaps.

## Data & Control Flow

Control moves through this folder into `ui/src/main/kotlin/co/anitrend/arch/ui/` and, when present, sibling resource codemaps.

## Integration Points

Use `ui/src/main/kotlin/co/anitrend/arch/ui/` for the meaningful Kotlin package map and nearby `res/` codemaps for Android resources.
